-- Script that deletes the database hbtn_0c_0 in your MySQL server.
-- Deletes the database without using SELECT or SHOW statements.
-- If database doesn't exists, script doesn't fail

DROP DATABASE IF EXISTS hbtn_0c_0;
